{
  "name": "vaultforge-wallet",
  "private": true,
  "version": "0.2.2",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1420",
    "generate-icon": "tauri icon src-tauri/icons/icon.svg -o src-tauri/icons",
    "prepare-tauri": "npm run sync-package-lock && npm run sync-cargo-version && npm run generate-icon",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "sync-package-lock": "npm install --package-lock-only --ignore-scripts --no-audit --no-fund",
    "sync-cargo-version": "node scripts/sync-cargo-version.mjs",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@tailwindcss/postcss": "^4.3.2",
    "@tauri-apps/cli": "^2.11.4",
    "@types/qrcode": "^1.5.6",
    "autoprefixer": "^10.5.2",
    "eslint": "^10.6.0",
    "eslint-plugin-unused-imports": "^4.4.1",
    "globals": "^17.7.0",
    "postcss": "^8.5.16",
    "tailwindcss": "^4.3.2",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.63.0",
    "vite": "^8.1.3"
  }
}
